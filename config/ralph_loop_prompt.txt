You are iterating on an ASL neural network to beat least-squares fitting on BOTH synthetic data AND real patient brain data. The in-vivo results are the immovable ground truth — you cannot modify in-vivo data or cached LS results. If synthetic says "great" but in-vivo says "bad", your synthetic pipeline is broken. Fix everything: model, training, synthetic data generation, preprocessing. Be bold.

THE GOAL:
NN must beat LS on ALL metrics in BOTH synthetic and in-vivo evaluation.
Results must be concordant — if synthetic says NN wins, in-vivo must agree.

CURRENT STATUS (NN is LOSING on in-vivo):
  Synthetic: NN can beat LS with domain randomization (established in prior work)
  In-vivo: NN LOSES to LS:
    - GM CBF CoV: NN=76.6% vs LS=46.4% (NN is NOISIER)
    - Spatial smoothness: NN=9.89 vs LS=7.71 (NN is ROUGHER)
    - GM CBF: NN=32.1 vs LS=37.3 (both low, expected 40-80)
    - Speed: NN=0.3s vs LS=33s (NN is 100x faster — the one win)
  The problem: synthetic evaluation doesn't predict in-vivo performance.

SETUP:
- Config: config/invivo_experiment.yaml
- Harness: python3 ralph_harness.py --device mps --n-epochs 30 --n-samples 3000
- Output: invivo_results/experiment_log.md, invivo_results/latest_results.json
- Each run: trains from scratch, evals synthetic (3 SNR, 10 phantoms), evals 3 in-vivo subjects
- ~15 min per iteration

READ-ONLY (DO NOT MODIFY — this is ground truth):
  - data/invivo_processed_npy/  (real patient signals, brain masks, GM masks)
  - invivo_comparison_results/  (cached LS results: ls_cbf.npy, ls_att.npy per subject)

You may modify ANY other file. Key ones:
  - config/invivo_experiment.yaml (hyperparams, architecture, physics, domain rand, noise)
  - ralph_harness.py (training, signal generation, in-vivo preprocessing, synthetic eval)
  - models/amplitude_aware_spatial_network.py (AmplitudeAwareSpatialASLNet)
  - models/spatial_asl_network.py (MaskedSpatialLoss, base U-Net)
  - simulation/enhanced_simulation.py (SpatialPhantomGenerator — tissue ranges, phantom structure)
  - simulation/noise_engine.py (NoiseInjector — noise models)

EACH ITERATION:
1. Read invivo_results/experiment_log.md and invivo_results/latest_results.json
2. Look at BOTH synthetic AND in-vivo results. Do they agree? If not, why?
3. Make changes. Multiple changes at once are fine.
4. Run: python3 ralph_harness.py --device mps --n-epochs 30 --n-samples 3000
5. Read results. If worse overall: revert with git checkout on changed files.
6. The experiment_log.md auto-appends each run.

LIKELY ROOT CAUSES (investigate these):

1. SIGNAL SCALE MISMATCH: In ralph_harness.py, in-vivo signals are scaled by
   M0*global_scale = 100*10 = 1000x. In-vivo brain signals P95 is ~0.006,
   so after scaling ~6. Training signals after scaling are ~1-10. Ranges roughly
   match but verify: print distributions at each step. Maybe in-vivo npy data
   is already partially scaled.

2. PHANTOM STRUCTURE: SpatialPhantomGenerator creates geometric blobs. Real brains
   have cortex, ventricles, sulci. The U-Net learns blob patterns that break on
   real anatomy. What spatial features does the model need? Maybe simpler is better.

3. NOISE MODEL: Training uses Gaussian. In-vivo has Rician + physio + motion.
   Model may learn wrong denoising.

4. NORMALIZATION CHAIN: z-score denorm uses training norm_stats. If training CBF
   distribution doesn't match in-vivo, absolute values are wrong. Model might
   produce correct RELATIVE patterns but wrong scale.

5. DOMAIN RANDOMIZATION: May not cover real patient variability.

DEBUGGING TIPS:
- Print signal stats in ralph_harness.py to compare training vs in-vivo distributions
- Check if model produces good RELATIVE spatial patterns (scaling issue vs model issue)
- Try different M0 scaling for in-vivo inference
- A152 (20231030_MR1_A152) is the best subject — start debugging there
- The smoke test with 5 epochs showed PASSING metrics — more training may overfit
  to synthetic phantom structure. Try fewer epochs or more diverse phantoms.

SUCCESS CRITERIA — ALL must pass:

1. SYNTHETIC (all tested SNR levels: 3, 10, 25):
   - CBF slope: 0.3 - 1.8
   - abs(CBF bias) < 15
   - CBF win rate > 50% (NN beats LS per-voxel)
   - ATT win rate > 50%

2. IN-VIVO (all evaluated subjects):
   - GM CBF in [15, 120] ml/100g/min
   - GM/WM ratio in [1.2, 6.0]
   - GM ATT in [400, 3000] ms
   - NN CoV < LS CoV (average across subjects)
   - NN smoothness < LS smoothness (average across subjects)

3. CONCORDANCE:
   - Synthetic says NN wins AND in-vivo says NN wins

When ALL criteria pass, output <promise>METRICS_PASSING</promise>.

If after 15 iterations you're stuck, write analysis to invivo_results/stuck_report.md
and output <promise>METRICS_PASSING</promise>.
