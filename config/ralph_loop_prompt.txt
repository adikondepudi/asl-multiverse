You are iterating on an ASL neural network to beat least-squares fitting for CBF and ATT estimation. You are smart. Make bold moves. Change multiple things at once if they make sense together. Trust your judgment.

SETUP:
- Working config: config/fast_experiment.yaml
- Test harness: python3 fast_train_validate.py --config config/fast_experiment.yaml --device mps
- Results log: fast_eval_results/experiment_log.md
- Latest results: fast_eval_results/latest_results.json
- You may modify ANY source file. Key ones:
  - config/fast_experiment.yaml (hyperparams, loss weights, architecture flags)
  - models/spatial_asl_network.py (MaskedSpatialLoss, SpatialASLNet)
  - models/amplitude_aware_spatial_network.py (AmplitudeAwareSpatialASLNet)
  - training/asl_trainer.py (training loop, noise injection)
  - simulation/enhanced_simulation.py (SpatialPhantomGenerator tissue ranges)
  - fast_train_validate.py (the harness itself if needed)

EACH ITERATION:
1. Read fast_eval_results/experiment_log.md and fast_eval_results/latest_results.json
2. Analyze ALL SNR levels, not just one. Understand the pattern.
3. Make changes. Multiple changes at once are fine if they address related issues.
4. Run: python3 fast_train_validate.py --config config/fast_experiment.yaml --device mps
5. Read the results. If worse overall: revert with git checkout on the files you changed.
6. The experiment_log.md auto-appends each run.

PHILOSOPHY:
- Don't be timid. If you see 3 things wrong, fix all 3.
- Read the code to understand WHY something is failing before changing it.
- The test harness trains for only 5 epochs on 1000 samples. That's enough to detect architecture/loss problems but not enough for full convergence. Judge trends, not absolute values.
- If win rate is low, think about what LS is doing right that the NN isn't capturing.
- The NN advantage should be strongest at low SNR (denoising) -- if you're losing at SNR=3 something is fundamentally wrong.

KNOWN FAILURE MODES:
- CBF variance collapse (slope < 0.3): model predicting constant -- increase variance_weight, check architecture
- CBF super-linearity (slope > 1.5): model overshooting high CBF -- bound outputs, expand training range
- CBF bias: check normalization chain end-to-end, M0 scaling, denormalization
- ATT MAE > 200ms: check att_scale, ATT target normalization
- Low win rate: think about what information the NN needs to beat LS. LS uses exact physics model -- NN needs enough capacity and correct training signal to match that.
- Loss not decreasing: learning rate, data issues, architecture too small/large

SUCCESS CRITERIA -- ALL must pass at EVERY SNR level (3, 5, 10, 15, 25):
- CBF linearity slope: 0.5 - 1.5 (no variance collapse, no super-linearity)
- abs(CBF bias) < 10.0 ml/100g/min
- ATT MAE < 300ms
- No FAIL kill signals

AND at SNR=10 specifically:
- CBF win rate vs LS: > 50% (the NN must BEAT least-squares)
- ATT win rate vs LS: > 50%

When ALL success criteria are met across ALL SNR levels, output <promise>METRICS_PASSING</promise>.

If after 15 iterations you are stuck on the same failure mode with no progress, write a detailed analysis to fast_eval_results/stuck_report.md explaining what you tried, what the fundamental blocker is, and what you think would fix it with more training time/data. Then output <promise>METRICS_PASSING</promise>.